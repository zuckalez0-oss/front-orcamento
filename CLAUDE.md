# CLAUDE.md — GeoQuote (Lypsyos)

Fonte de verdade sobre arquitetura e decisões de projeto. Para instruções de setup/instalação, ver `README.md`.

## Stack

- **Backend**: FastAPI + SQLAlchemy 2.x. Banco configurável via `DATABASE_URL` (Postgres do projeto Supabase em produção; sem a variável definida, cai num SQLite local `backend-orcamentos/geoquote.db`, só pra dev/teste sem credenciais reais — ver seção "Contas, Assinaturas e Times" abaixo pra por que a migração de SQLite fixo pra Postgres configurável aconteceu). Modelos/rotas CRUD/motor de cálculo/DXF (`ezdxf`) em `main.py`; nesting 2D (`rectpack`) isolado em `nesting.py`; fórmulas de tempo/custo em `pricing.py`; geometria de formas padrão (R/Q/C/T) em `geometria.py`.
- **Frontend**: React 19 + Vite + Tailwind v4. Tela principal ainda é um componente único `front-orcamentos/src/App.jsx`, sem roteador — telas controladas por state: `formulario`, `parametrizacao`, `resultado`, `perfil`, `historico`, `planos`. Parte da lógica já foi extraída pra módulos satélite conforme cresceu: `RapidEntryGrid.jsx` (Entrada Rápida em grade), `pecaUtils.js` (construção do objeto peça, compartilhado entre Formulário e Entrada Rápida), `nestingUtils.js` (funções puras de geometria/transformação compartilhadas entre a tela e o PDF), `nestingPdf.js` + `relatorioIndustrialPdf.js` (exportação do plano de corte em PDF, ver seção própria abaixo), `Historico.jsx`/`Planos.jsx`/`PerfilUsuario.jsx` (telas de conta, ver seção "Contas, Assinaturas e Times"). Estilos utilitários Tailwind + um punhado de classes customizadas em `App.css`. PDF gerado com `jspdf` + `jspdf-autotable`.
- **Duas camadas de identidade coexistem no mesmo projeto Supabase, de propósito separadas** (ver seção "Contas, Assinaturas e Times" abaixo): o RBAC interno da equipe Lypsyos (`ADMINISTRADOR/GERENTE/VENDEDOR`, tabela `profiles`, sem autocadastro) e as contas de assinatura Free/Pro/Enterprise dos clientes externos (`contas`/`contas_usuarios`, autocadastro público). O GeoQuote hoje serve os dois públicos ao mesmo tempo, na mesma tela de login.

## Rodando localmente

```bash
cd backend-orcamentos && source venv/bin/activate && uvicorn main:app --reload   # http://localhost:8000
cd front-orcamentos && npm run dev                                              # http://localhost:5173
```

O frontend chama a API via URL hardcoded `http://localhost:8000` (constante `API_BASE` em `front-orcamentos/src/apiBase.js`, compartilhada entre `App.jsx` e `AuthGate.jsx`).

Backend precisa de `backend-orcamentos/.env` (copiar de `.env.example`) com `DATABASE_URL` e `SUPABASE_JWT_SECRET` pra funcionar com contas/autenticação de verdade — sem isso, cai no fallback SQLite e toda rota autenticada responde 500 (JWT não tem como ser validado). Ver seção "Contas, Assinaturas e Times".

## Schema do banco de dados

Quatro tabelas de parâmetro, propositalmente separadas (ver "Decisões arquiteturais" abaixo), todas escopadas por `conta_id` (ver seção "Contas, Assinaturas e Times"):

- **`materiais`** (`MaterialDB`): `id, conta_id, perfil_tributario_id, nome, espessura, precoKg, densidade`. Chave natural de upsert: `(perfil_tributario_id, nome, espessura)` — reparem que NÃO inclui `conta_id` sozinho como no resto: o preço é ligado ao Perfil Tributário, não direto à conta, de propósito (ver por quê na seção de Contas).
- **`maquinas_params`** (`MaquinaParamDB`): `id, conta_id, maquina, material, espessura, velocidadeCorte, tempoPiercing, tempoSetup, valorHora`. Chave natural de upsert: `(conta_id, maquina, material, espessura)`. O campo `material` é um link "fraco" (string) para `materiais.nome` — não há FK real.
- **`perfis_tributarios`** (`PerfilTributarioDB`): `id, conta_id, nome, imposto_perc, padrao`. Chave natural de upsert: `(conta_id, nome)`. `padrao` marca qual perfil a conta usa quando o usuário não escolhe nenhum (no máx. 1 `True` por conta, aplicado em `add_perfil`).
- **`clientes`** (`ClienteDB`): `id, conta_id, nome` — lista simples (autocomplete do dropdown "Cliente" no formulário), populada automaticamente a cada `/calcular-orcamento` bem-sucedido (`upsert_cliente`), sem cadastro próprio.

Todas as rotas `POST /materiais`, `POST /maquinas-params`, `POST /perfis-tributarios`, `POST /clientes` fazem upsert pela chave natural (busca por essa chave dentro da conta do usuário autenticado; se existe, atualiza; senão, cria). `Base.metadata.create_all()` roda no import do módulo — em Postgres de produção isso é um no-op pras tabelas que já existem via `supabase/schema_contas.sql`; só cria de fato num SQLite local vazio. Não existe mais seed global automático (fazia sentido quando só havia 1 banco compartilhado) — cada conta nova ganha só um Perfil Tributário "Padrão" via `seed_conta_padrao`, chamado no provisionamento (`POST /contas/provisionar`).

As dimensões de chapa (`largura`/`comprimento`/`margem`/`offsetPeca`, e também `maquina` e `clienteQuerSobra` por espessura — ver seção Nesting) **não são persistidas**: são digitadas/marcadas no modal "Dimensões de Chapa" a cada orçamento e viajam no payload de `/calcular-orcamento`. Isso é intencional, mesmo padrão usado desde antes deste documento existir — chapa/margem/offset/máquina/sobra são tratados como parâmetros por-orçamento, não cadastro fixo. O orçamento inteiro (payload + resultado), esse sim, agora é persistido — ver `orcamentos` na seção de Contas.

## Decisões arquiteturais

- **Por que 3 tabelas de parâmetros em vez de uma:** o cadastro original misturava Preço/Kg (muda toda semana, é decisão de compras) com Velocidade/R$-hora (quase fixos, decisão de engenharia) num único registro por combinação máquina+material+espessura. Isso obrigava reeditar registro por registro sempre que o aço mudava de preço. Separar em Material (mercado) / Máquina-Corte (engenharia) / Perfil Tributário (faturamento) deixa cada tabela mudar no seu próprio ritmo. O join entre `materiais` e `maquinas_params` acontece no frontend (`parametroAtual` em `App.jsx`), não no backend.
- **Por que o dropdown de Espessura às vezes aparece vazio:** ele reflete apenas combinações Máquina+Material que já têm uma linha em `maquinas_params`. Cadastrar uma espessura só em `materiais` (aba Matéria-prima) não a torna selecionável até existir também o parâmetro de corte correspondente em `maquinas_params` (aba Máquina & Corte). O frontend avisa quando essa lacuna existe (ver aba "Máquina & Corte" e mensagens de estado vazio no formulário de peça).
- **Nesting é retangular na camada de bin-packing, não poligonal genérico:** a alocação de itens na chapa (`rectpack`) continua operando só sobre bounding boxes (mesmo quando a peça é importada de DXF — `nesting.py` só recebe `dimA`/`dimB`, nunca o contorno). NFP (No-Fit-Polygon) genérico — colisão polígono-a-polígono arbitrária, via Shapely/pyclipper — foi avaliado e descartado de novo por complexidade/tempo (é um projeto de pesquisa em si). **Exceção parcial, deliberada:** triângulos (`tipoPeca == 'T'`) têm identidades geométricas EXATAS de ladrilhamento — dois triângulos "reto" pareados ao longo da hipotenusa ladrilham um retângulo `base x altura` com 0% de desperdício; triângulos "isósceles" ladrilham em fileira alternada (apex-up/apex-down, passo `base/2`) com aproveitamento exato `N/(N+1)` para N peças na fileira. Esse pré-processamento (`nesting.py::_compor_itens_triangulo`) roda ANTES do `rectpack` e entrega blocos compostos já densos — é matemática fechada, não busca/heurística, então não reabre a questão do NFP genérico. Ver `backend-orcamentos/nesting.py`.
  - **Mas a RENDERIZAÇÃO de uma peça DXF já posicionada usa o contorno real**, não mais um retângulo genérico — isso é só desenho, não muda a decisão de bin-packing acima. Ver seção "Contorno real do DXF" abaixo.
- **Tema visual:** paleta definida em `App.css` (`--gq-dark-bg`, `--gq-navy-panel`, `--gq-orange-accent`, `--panel-gradient`) é o design system "oficial" da marca (laranja + preto). Historicamente só o header usava o tema escuro; a tela de inserção de peça foi migrada para usar `.panel-dark`/`.input-dark` (ver `App.css`) para ficar consistente com essa identidade. As telas "Parâmetros Globais" e "Resultado"/impressão permanecem no tema claro por enquanto.

## Contas, Assinaturas e Times (SaaS multi-tenant)

O GeoQuote deixou de ser só a ferramenta interna da Lypsyos e passou a também ser vendido como produto self-service, em 3 planos: **Free** (10 orçamentos/dia, 50 peças/orçamento, 1 usuário), **Pro** (ilimitado, 1 usuário) e **Enterprise** (ilimitado, 1 GESTOR + N VENDEDORES convidados, assentos configuráveis). Essa seção documenta o estado atual dessa camada — o que já funciona de ponta a ponta, o que é deliberadamente simplificado, e o que falta.

### Modelo de identidade: dois sistemas paralelos, não um só

`supabase/schema.sql` (RBAC interno, `profiles`, sem autocadastro) e `supabase/schema_contas.sql` (contas de assinatura, `contas`/`contas_usuarios`/`convites`/`orcamentos`/`uso_diario`, autocadastro público) vivem no MESMO projeto Supabase/`auth.users`, mas são tabelas e conceitos **totalmente separados** — decisão deliberada pra não misturar "conta interna da Lypsyos" com "cliente pagante". Um usuário pode, em teoria, ter linha nas duas (foi o caso da própria equipe Lypsyos depois da migração, ver "Migração" abaixo), mas a maioria só tem uma.

No frontend, `AuthContext.jsx`/`AuthGate.jsx` carregam os dois em paralelo e expõem via `useAuth()`: `perfil`/`papel` (RBAC interno, valores `ADMINISTRADOR/GERENTE/VENDEDOR`) e `contaAtual`/`papelConta` (conta de assinatura, valores `PROPRIETARIO/GESTOR/VENDEDOR`). **Atenção**: `VENDEDOR` existe nos dois sistemas com significados diferentes — por isso os nomes de campo (`papel` x `papelConta`) são propositalmente distintos, nunca reaproveite um pelo outro.

No backend, `get_usuario_atual` (dependency em `main.py`) é o único ponto que valida o JWT do Supabase (`Authorization: Bearer`, `SUPABASE_JWT_SECRET`, HS256) e carrega `contas_usuarios` — **não olha pra `profiles`/RBAC interno**. Toda rota escopada por conta (`materiais`, `maquinas-params`, `perfis-tributarios`, `clientes`, `calcular-orcamento`, `orcamentos`, `contas/*`) depende dele; 401 se o token for inválido, 403 se o usuário autenticado não tiver linha em `contas_usuarios` ainda.

### Provisionamento — por que NÃO é um trigger em `auth.users`

Diferente de `handle_new_user()` (que cria `profiles` automaticamente pra toda conta nova, em `schema.sql`), criar `contas`/`contas_usuarios` **não** é feito por trigger. Um trigger em `auth.users` não teria como distinguir "conta interna da Lypsyos criada manualmente pelo admin" de "cadastro público novo" — errar isso faria todo admin/vendedor interno ganhar de graça uma assinatura Free e ser limitado a 10 orçamentos/dia, quebrando o uso interno. Em vez disso: `POST /contas/provisionar` (autenticado só pelo JWT, via `get_identidade_jwt` — não exige `contas_usuarios` já existir, já que é ele quem cria) é chamado explicitamente pelo frontend depois de um cadastro público (`Login.jsx::handleCriarConta`), e é idempotente (chamar de novo pra quem já tem conta só devolve o que já existe). Aceita um `convite_token` opcional — se vier, a pessoa entra na conta do convite como VENDEDOR em vez de ganhar conta própria (fluxo Enterprise).

**Caso especial já resolvido**: se o projeto Supabase exige confirmação de e-mail, `signUp()` devolve sessão nula — não dá pra provisionar na hora. `Login.jsx` grava um marcador em `localStorage` (`geoquote_provisionamento_pendente`, sobrevive à troca de aba do clique no link do e-mail) e `AuthGate.jsx::carregarConta` o consome assim que uma sessão de verdade aparece, terminando o provisionamento sozinho.

**Contas internas da Lypsyos (`profiles`, criadas manualmente no painel Supabase — nunca chamam `/contas/provisionar`) também precisam de uma linha em `contas_usuarios`**, senão ficam 403 em toda rota escopada por conta assim que essas rotas passaram a exigir `conta_id`. Em vez de depender de alguém lembrar de rodar `migrar_para_postgres.py` toda vez que uma conta interna nova aparece, isso é **self-healing**: `get_usuario_atual::_autoprovisionar_conta_interna` (main.py) — se não existe `contas_usuarios` pro uid do token mas existe uma linha em `profiles`, provisiona na hora, ligando à conta fixa `LYPSYOS_CONTA_ID_PADRAO` (UUID fixo, sempre o mesmo, criada na primeira vez que isso acontece) com o papel mapeado (`ADMINISTRADOR/GERENTE → GESTOR`, `VENDEDOR → VENDEDOR`). Um usuário sem `profiles` **e** sem `contas_usuarios` continua 403 normalmente (não inventa conta do nada). `migrar_para_postgres.py` usa essa mesma constante como destino padrão, então continua útil pra migrar de uma vez os dados de negócio antigos do SQLite — só não é mais pré-requisito pra equipe interna conseguir logar.

### Preço de matéria-prima por perfil tributário (item que motivou o `perfil_tributario_id` em `materiais`)

Pedido original: numa conta Enterprise, dois vendedores podem precisar do mesmo material/espessura com preços diferentes. Resolvido ligando `materiais` a `perfis_tributarios` (não direto a `conta_id`) — cada perfil carrega seu próprio conjunto de preços; a conta toda compartilha os mesmos `maquinas_params` (parâmetro de engenharia, não de preço). Na tela de Parametrização, a aba "Matéria-prima" tem um seletor de Perfil Tributário no topo (reaproveita o state `perfilSelecionadoId`, que já existia só pra resolver `imposto`) — troca de perfil filtra a lista e decide em qual perfil o próximo material cadastrado entra. **Ponto de atenção se for mexer aqui de novo**: `App.jsx::materialAtual` (usado pra resolver o preço da peça sendo montada no Formulário) filtra por `perfil_tributario_id === perfilSelecionadoId` além de nome/espessura — sem esse filtro, com materiais duplicados entre perfis, o `.find()` pegaria a primeira linha que aparecesse, não necessariamente a do perfil certo.

### RBAC de edição de parâmetros

`exigir_papel_edicao` (backend) bloqueia `POST`/`DELETE` em materiais/máquinas/perfis pra `papel == VENDEDOR`; GET continua liberado pra qualquer membro da conta (vendedor precisa VER preço pra montar orçamento, só não pode mudar). No frontend, `App.jsx::podeEditarParametros` espelha essa regra pra esconder formulários/botões Editar/Excluir de VENDEDOR (mostra só leitura) — e `podeAcessarParametrizacao` (= `papel === 'ADMINISTRADOR' || !!papelConta`) é o que faz a tela de Parametrização existir pra qualquer membro de conta, não só pro RBAC interno (bug real que existia até essa rodada: o gate antigo checava só `papel === 'ADMINISTRADOR'`, o que deixava TODO cliente público sem acesso nenhum à própria tela de preços).

### Cotas do plano Free

`verificar_quota` (dependency em `/calcular-orcamento`) e `uso_diario` (contador `usuario_id + dia`, resetado sozinho por virar a data, sem cron): bloqueia acima de 50 peças por orçamento (400) e acima de 10 orçamentos no dia (429), só pra `tipo_plano == FREE` — Pro/Enterprise não têm teto. `registrar_uso_diario` só incrementa depois de um cálculo bem-sucedido (tentativa que falhou em validação/nesting não consome cota). `GET /contas/me` devolve `orcamentos_hoje`/`limite_orcamentos_dia` pra UI mostrar contador; `App.jsx` mostra um aviso não-bloqueante quando a lista de peças passa de 50 antes mesmo de tentar calcular.

### Orçamentos: histórico, fechamento, tempo real, métricas

Todo `/calcular-orcamento` bem-sucedido grava uma linha em `orcamentos` (`OrcamentoDB`): `payload` guarda o pedido enviado + resultado devolvido como JSON (não normalizado peça-a-peça — permite reabrir/reexportar um orçamento antigo sem tabela própria por peça). `PATCH /orcamentos/{id}/fechar` marca `FECHADO` (só o vendedor dono ou o GESTOR da conta podem); é o gatilho de "orçado x fechado" pedido pelo usuário. `GET /orcamentos` (histórico) e `GET /contas/metricas` (agregados + por vendedor) fazem PROPRIETARIO/VENDEDOR verem só os próprios, GESTOR ver a conta inteira.

**Tempo real pro gestor** é 100% Supabase Realtime, zero código de push no FastAPI: a tabela `orcamentos` está na publicação `supabase_realtime` (ver fim de `schema_contas.sql`) e tem RLS ("gestor vê os orçamentos de toda a conta") — `Historico.jsx` abre `supabase.channel(...).on('postgres_changes', {event:'UPDATE', table:'orcamentos', filter:'conta_id=eq...'}, ...)` só quando `papelConta === 'GESTOR'`, e recebe o evento assim que o backend grava `status='FECHADO'` (Realtime capta a escrita por replicação lógica, não importa que quem escreveu foi o FastAPI com a `service_role` key).

### Planos: self-service, sem gateway de pagamento

`POST /contas/alterar-plano` troca `tipo_plano` (e `limite_vendedores`, obrigatório se virar Enterprise) **na hora, sem cobrança real** — decisão deliberada (não há gateway de pagamento integrado ainda) pra deixar testar o comportamento de cada tier já. Virar Enterprise promove automaticamente PROPRIETARIO → GESTOR. `Planos.jsx` é a tela de comparação Free/Pro/Enterprise com os botões de troca. **Quando um gateway (Stripe ou outro) for definido**, essa rota deixa de ser chamada direto pelo clique do botão e passa a rodar só depois de um checkout confirmado (webhook) — o campo `tipo_plano` em si não muda de forma.

### Convites (Enterprise)

`POST /contas/convites` (só GESTOR, valida contra `limite_vendedores`) gera um token opaco de uso único; o link (`/?convite=TOKEN`) abre `Login.jsx` direto na aba "Criar conta" com o token capturado da URL, e o cadastro consome o convite em vez de criar conta nova. Sem infra de e-mail hoje — o GESTOR copia/cola o link manualmente (`PerfilUsuario.jsx`, seção Equipe). Envio automático por e-mail é melhoria futura, não bloqueante.

### Migração de dados existentes (opcional)

`backend-orcamentos/migrar_para_postgres.py` — roda depois de `schema.sql` + `schema_contas.sql` já terem sido executados no Supabase e `DATABASE_URL` apontando pra lá. Migra os dados de negócio do SQLite antigo (materiais/máquinas/perfis/clientes) pra `LYPSYOS_CONTA_ID_PADRAO` (materiais sem perfil viram todos "Perfil Padrão") e faz de uma vez a ponte de todas as linhas de `profiles` pra `contas_usuarios`. **Não é mais pré-requisito** pra equipe interna conseguir usar o app — isso agora acontece sozinho por conta, na hora, via `_autoprovisionar_conta_interna` (ver "Provisionamento" acima). Continua útil só pra não perder os dados de negócio que já existiam no SQLite compartilhado antigo, e pra provisionar todo mundo de uma vez em vez de ir um por um.

### Bug já resolvido: enum nativo do Postgres x parâmetro vinculado do SQLAlchemy

`contas.tipo_plano`, `contas_usuarios.papel` e `orcamentos.status` eram `enum` nativos do Postgres na primeira versão de `schema_contas.sql`. O backend grava esses campos como string comum via parâmetro vinculado (psycopg) — e Postgres **não** faz cast implícito de um bind param pra um tipo enum customizado (só funciona com literal solto direto no SQL, tipo `'FREE'` escrito à mão). Resultado: toda escrita nessas 3 colunas (incluindo o auto-provisionamento de conta interna, ver acima) derrubava a request com 500. Não foi pego pelos testes porque eles rodam contra o fallback SQLite, que não tem enum — só apareceu testando contra o Postgres real.

**Corrigido** trocando esses 3 campos por `text` + `CHECK` (mesma validação, sem o problema de cast) — `schema_contas.sql` já nasce assim agora. Se seu projeto já rodou a versão antiga (com `create type ... as enum`), rode `supabase/schema_contas_fix_enums.sql` uma vez (converte as colunas + redefine `get_meu_papel_conta()` pra retornar `text` + dropa os tipos enum, que ficam órfãos depois da conversão).

### O que falta / simplificações conhecidas

- **Login por Google**: preparado (botão em `Login.jsx`, trigger de provisionamento já funciona com qualquer provedor de identidade — não olha COMO o usuário logou), mas o provider ainda não foi habilitado no painel Supabase.
- **Upload de foto de perfil**: campo `foto_url` existe em `contas_usuarios`/`PATCH /contas/me`, mas não há UI de upload nem bucket do Supabase Storage criado — precisa de um bucket `avatars` (ação manual no painel, como as migrações SQL) antes de implementar.
- **Gateway de pagamento**: nenhum integrado; troca de plano é self-service sem cobrança (ver acima).
- **`SUPABASE_JWT_SECRET`**: assume o esquema legado HS256 (segredo compartilhado). Projetos Supabase mais novos podem usar só o esquema assimétrico (JWKS) — se for o caso, `_decodificar_token` em `main.py` precisa trocar pra validação via chave pública/JWKS em vez do segredo fixo.
- **`orcamentos.payload`** guarda o orçamento inteiro como JSON, não normalizado — suficiente pra reabrir/consultar, mas não dá pra fazer queries agregadas por peça/material sem carregar o JSON inteiro. Ok pelo volume atual; reconsiderar se precisar de relatórios peça-a-peça no futuro.

## Motor de orçamento (`POST /calcular-orcamento`)

Exige autenticação (`Authorization: Bearer`, ver `get_usuario_atual`/`verificar_quota` na seção "Contas, Assinaturas e Times") — aplica cota do plano Free, registra o cliente em `clientes` e, ao final, grava o payload + resultado inteiro em `orcamentos` (histórico/fechamento/métricas, mesma seção).

Recebe `cliente, imposto, comissao, margemLucro, frete, pecas[], configChapas{espessura: {largura, comprimento, margem, offsetPeca, maquina, clienteQuerSobra}}, incluiMaterial`. Para cada peça calcula tempo de corte (perímetro/velocidade + piercing por furo + movimento vazio por furo + setup único por espessura — ver `pricing.py::tempo_producao_unidade_min`, já considera nº de furos reais e não só o perímetro externo) e custo de material (peso × preço/kg). Agrupa por espessura, roda nesting real (`nestear_pecas` em `nesting.py`) para determinar quantas chapas são necessárias, e aplica markup/comissão/imposto sobre o custo de produção total. `imposto` chega pronto do frontend (já resolvido a partir do Perfil de Faturamento escolhido). Velocidade de corte/tempo de piercing já variam por máquina+material+espessura via `maquinas_params` — não é um valor fixo.

O campo `fatorNesting` do payload é herdado do modelo antigo (heurística área/eficiência) e não é mais usado — a contagem de chapas agora vem 100% do nesting real. Mantido no schema só para não quebrar o frontend; pode ser removido dos dois lados no futuro se ninguém mais depender dele.

Cada item de `detalhamento_espessuras` no retorno inclui, além dos totais de custo/tempo: `chapa_largura`, `chapa_comprimento`, `chapa_margem` (os valores efetivamente usados, já com fallback para 1200x3000/10mm se o frontend não mandou `configChapas` para aquela espessura), `maquina` (a que efetivamente foi usada no nesting — override de `configChapas[esp].maquina` ou, na ausência, a da primeira peça do grupo), `sobra_reservada_cliente` (bool, de `configChapas[esp].clienteQuerSobra`), métricas de **Controle de Sobras** (`chapa_area_total_mm2`, `kerf_perda_area_mm2`, `sucata_area_mm2`, `sucata_peso_kg`, `utilizacao_pct` — informativas; o custo de material já fatura a chapa inteira, então a sobra já está paga, isso não gera cobrança nova) e `nesting: {chapas_necessarias, chapas: [{placements: [{id, x, y, width, height, rotated, invertido}]}]}` — coordenadas já absolutas na chapa real (mm), prontas para desenhar sem transformação adicional no frontend. `totais_globais` tem os agregados equivalentes: `sucata_peso_total_kg`, `sucata_area_total_mm2`, `utilizacao_media_pct`, `custo_material`.

A resposta também traz `detalhamento_pecas` (dict indexado por `peca.id`, com `tempo_min`/`custo_maquina` **já calculados por peça individual** — não é rateio/estimativa, é o mesmo cálculo que já alimentava os agregados por espessura, só que exposto por peça) e `inclui_material` (eco do flag do payload). Usados na tabela impressa do orçamento (`App.jsx`, seção "TABELA DE PEÇAS - UNIFICADA") pra preencher as colunas Chapas/Tempo/Custo Máquina por linha, que antes ficavam sempre com "-".

**Facção x pacote completo (`incluiMaterial`):** checkbox no topo do formulário (`App.jsx`, estado `incluiMaterial`, default `true`). Quando `false`, o backend ainda calcula `chapas_necessarias`/`dimensao_chapa`/peso normalmente (informativo, pro orçamentista saber quanto material o cliente precisa trazer) mas **zera** `dados_esp["custo_material"]` e não soma em `custo_material_global` — o markup passa a incidir só sobre o custo de máquina/serviço. Refletido na UI como um selo "Pacote completo" / "Só serviço (facção)" no resumo e no cabeçalho do PDF impresso, e um card "Custo Material R$" nos totais (mostra R$ 0,00 quando é facção).

### Nesting (`backend-orcamentos/nesting.py`)

`nestear_pecas(pecas, largura_chapa, comprimento_chapa, margem, offset_peca, maquina=None)` — usa `rectpack` (`newPacker(rotation=True)`, multi-bin; `maquina == "GUILHOTINA"` troca o algoritmo de empacotamento, ver comentário no código). Peças não-triângulo (R/Q/C, e também DXF — ver seção abaixo) são expandidas em `qtd` retângulos individuais `(dimA + offset_peca) x (dimB + offset_peca)`, como sempre. Triângulos (`tipoPeca` em `pecas[i]`, precisa vir junto de `tipoTriangulo`) passam primeiro por `_compor_itens_triangulo`, que agrupa por `(tipoTriangulo, base, altura)` idênticos e devolve pares/fileiras compostos (ver decisão arquitetural acima) — só esses itens compostos (ou a sobra individual, se ímpar) chegam ao `packer.add_rect`. Depois do `packer.pack()`, um item composto é expandido de volta em N `placements` (um por triângulo membro), usando a mesma transformação de rotação 90° já usada para o item inteiro; cada placement de triângulo ganha `invertido: bool` (indica se o desenho deve usar o conjunto de vértices complementar — ver `verticesTriangulo` em `nestingUtils.js`). O padding (`offset_peca`) é subtraído de volta na posição final do item (par/fileira ou peça avulsa) para que o espaçamento visual entre itens seja exatamente `offset_peca` — mas NÃO existe padding entre os membros DENTRO de um item composto (eles interligam sem vão, por design). Levanta `NestingError` (capturada em `main.py` e convertida em `HTTPException 400`, nunca 500) quando: a margem é maior que a própria chapa, um item (peça avulsa ou par/fileira de triângulo) não cabe na área útil nem rotacionado, ou o total de itens pós-composição de uma espessura passa de `LIMITE_INSTANCIAS` (2000 — trava de segurança para não travar a API síncrona com lotes gigantes; pares/fileiras contam como 1 item cada, não pela contagem bruta de peças físicas). O frontend (`handleSalvar` em `App.jsx`) mostra o `detail` desse erro 400 direto num `alert()` e reabre o modal de configuração de chapa.

## Importação de DXF

- **Múltiplos arquivos de uma vez**: tanto o Formulário quanto a Entrada Rápida (`RapidEntryGrid.jsx`) aceitam selecionar vários `.dxf` de uma vez (`<input type="file" multiple>`). No Formulário, 1 arquivo popula o form pra revisão (fluxo antigo); 2+ arquivos importam cada um direto como peça própria (qtd=1, usando a máquina/material/espessura selecionados no momento), com resumo de sucesso/falha por arquivo. O identificador da peça vem do nome do arquivo (sem `.dxf`).
- **Importação por JSON**: botão próprio na Entrada Rápida (`RapidEntryGrid.jsx::parseJsonPecas`) — aceita um array de peças (ou `{"pecas": [...]}`) com campos `id/geometria/dimA/dimB/qtd/maquina/material/espessura` e furação (`tipoFuro/nFuros/diaFuro/furoOffsetX/furoOffsetY`). Mais expressivo que o CSV (que também existe, sem furação).
- **Edição em massa na Entrada Rápida** (`RapidEntryGrid.jsx`, estado `linhasSelecionadas`): cada linha ganhou um checkbox (+ um "selecionar todas" no cabeçalho). Hoje só o campo Máquina participa disso — se a linha que disparou o `onChange` faz parte de uma seleção com mais de 1 linha marcada, o novo valor se aplica a TODAS as linhas marcadas (cada uma recalculando seu próprio material/espessura padrão pra máquina nova, igual já acontecia numa edição de linha única). Ver `atualizarLinha`. Generalizável pra outros campos no futuro se pedirem — o mecanismo (`chavesAlvo` = seleção quando aplicável, senão só a linha corrente) já está pronto pra isso.
- **Contorno real do DXF**: `/processar-dxf` (`main.py`) extrai, além do `svgMarkup` (preview visual, mantido como estava) e das métricas de bounding box, um campo **aditivo** `contorno: {perfis: [{pontos: [[x,y],...], fechado}], furos: [{cx,cy,r}]}` — normalizado no referencial local `[0,dimA]x[0,dimB]`, origem no canto superior-esquerdo, mesma convenção já usada por `furosAbsolutos`/`verticesTriangulo` em `nestingUtils.js`. Só `LWPOLYLINE`/`POLYLINE`/`LINE`/`CIRCLE` são tratados (mesma limitação que o `svgMarkup` sempre teve); `ARC`/`SPLINE` não geram perfil — a peça cai de volta no retângulo da bounding box, sem quebrar nada.
  - Esse contorno **nunca vai pro backend em `/calcular-orcamento`** — vive só no objeto peça do frontend (`pecaUtils.js::construirPeca` grava em `contornoDxf`, restaurado em `editarPeca`/threading de import em lote/Entrada Rápida do mesmo jeito que `dxfPreviewSvg`). A renderização (tela `ChapaSVG` em `App.jsx` e PDF `nestingPdf.js`) já tem os placements com `id`, então busca a peça original em `listaPecas` e usa o contorno local dela — não precisa que o backend devolva nada a mais.
  - Transform de posicionamento (`nestingUtils.js::contornoDxfAbsoluto`): mesma identidade de rotação 90° usada pra furos/triângulos — `xAbs = rotated ? (dimB - y) : x; yAbs = rotated ? x : y`, depois translada por `placement.x/y`.
  - Heurística pra múltiplos perfis fechados (peça com rasgos internos desenhados como polilinha fechada, não só `CIRCLE`): o perfil de maior bounding box é o contorno EXTERNO (preenchido com a cor da peça); os demais fechados são "buracos" (preenchidos com o fundo, mesmo tratamento visual dos furos circulares); perfis abertos residuais só são traçados. Não é topologia CAD genérica, é uma simplificação pragmática — documentada assim no código (`nestingUtils.js::contornoDxfAbsoluto`, `nestingPdf.js`).
  - `furosAbsolutos` (`nestingUtils.js`) prefere os furos reais do DXF (`pecaOriginal.contornoDxf.furos`) quando existem, em vez da posição procedural calculada por `tipoFuro`/offsets (que é só aproximação pra peça desenhada à mão).

## PDF do Plano de Corte (nesting)

Layout reescrito num padrão "industrial" (inspirado num relatório de plano de corte real de outro sistema do setor — cabeçalho com logo/máquina/data, painel por espessura, plano de corte, especificações da chapa, lista de peças, sobras) pensado pra ser reutilizado por outras ferramentas de orçamento do nicho ferro/aço no futuro — por isso ficou em **dois módulos**:

- **`front-orcamentos/src/relatorioIndustrialPdf.js`** — o template genérico e reutilizável. Não conhece nada de "orçamento"/"peça"/"nesting" do GeoQuote, só desenha seções a partir de dados já resolvidos (texto/número simples), usando `jspdf-autotable` (dependência nova) pras tabelas com borda. Exporta uma função por seção (`desenharCabecalhoIndustrial`, `desenharPainelEspessura`, `desenharTituloPlanoDeCorte`/`desenharBarraTitulo`, `desenharTabelaEspecificacoesChapa`, `desenharTabelaPecasDoPlano`, `desenharTabelaSobras`, `desenharRodapeIndustrial`), cada uma recebendo `doc` + Y atual e devolvendo o próximo Y. Logo é um placeholder desenhado (`desenharLogoPlaceholder`) trocável em runtime via `configurarLogoRelatorio(dataUrlBase64)` sem mexer em mais nada.
- **`front-orcamentos/src/nestingPdf.js`** — o orquestrador GeoQuote-específico (mesma assinatura pública de antes: `gerarPdfNesting(resultadoOrcamento, listaPecas, cliente, espessuraFiltro, orientacao = 'retrato')`). Monta os dados e desenha o diagrama da chapa em si (que é nesting-específico: cores, contorno real, furos — reaproveita `nestingUtils.js`).

Pontos de design que valem lembrar antes de mexer aqui:

- **Chapas com arranjo idêntico viram 1 página só, com multiplicador "(NX)"** (`agruparChapasIdenticas`/`assinaturaChapa` em `nestingPdf.js`) — assinatura = lista ordenada de `(id, x, y, rotated)` por chapa; chapas com a mesma assinatura são o mesmo "Plano de Corte", só repetido N vezes fisicamente. Evita 1 página por chapa física quando o pedido tem muitas chapas repetidas. Cada título de plano também mostra a posição dentro do total de planos DAQUELA espessura, formato `(N-TOTAL)` — ex: espessura com 3 planos gera "PLANO DE CORTE 1 (1-3)", "...2 (2-3)", "...3 (3-3)" — ajuda o orçamentista a saber quantas páginas faltam sem contar manualmente (`desenharTituloPlanoDeCorte` em `relatorioIndustrialPdf.js`, parâmetro `totalPlanos`; combina com o `(MX)` de multiplicador quando os dois se aplicam).
- **Altura do diagrama é FIXA por orientação de página** (`ALTURA_DIAGRAMA_RETRATO_MM = 125`, `ALTURA_DIAGRAMA_PAISAGEM_MM = 85`), não calculada a partir do que sobra depois das tabelas — isso é proposital, garante que toda página de plano de corte tenha o mesmo tamanho de diagrama, independente de quantos tipos de peça ou sobras aparecem embaixo. Se as tabelas de peças/sobras não couberem no resto da página, o `autoTable` continua numa página seguinte com o cabeçalho de colunas repetido (`showHead: 'everyPage'` em `desenharTabelaPecasDoPlano`/`desenharTabelaSobras`) em vez de cortar a tabela sem contexto.
- **Diagrama sempre desenhado "deitado"**: quando `chapa_comprimento > chapa_largura` (chapa "em pé", ex: 1200x3000), o desenho inteiro é rotacionado 90° (`girar` em `desenharDiagramaChapa`, funções `pontoRotacionado90`/`retanguloRotacionado90`) — o eixo mais comprido sempre fica horizontal, porque a área do diagrama é sempre bem mais larga que alta (independente de a PÁGINA estar em retrato ou paisagem), então "deitar" a chapa aproveita muito melhor o espaço do que desenhar do jeito "natural". É só transformação de desenho — não mexe em `nesting.py`/nos placements reais.
- **Cota** (`desenharCotaChapa`): linha com marcas nas pontas embaixo (largura do desenho) e à direita (altura do desenho) da chapa, com a medida real em mm — não o valor escalado. O espaço reservado é MEDIDO de verdade (`medirReservaCotaVertical`, usa `doc.getTextWidth()` no texto real "NNNN mm" antes de calcular a escala), não um valor fixo chutado — já teve dois bugs em cima disso: (1) reserva fixa pequena demais fazia a chapa cobrir o número; (2) o texto da cota vertical era desenhado ROTACIONADO 90° (`angle:90`), o que se mostrou pouco confiável (saía sobreposto pela própria chapa em alguns casos, provavelmente por causa de como jsPDF ancora texto rotacionado) — trocado por texto HORIZONTAL normal ao lado da linha vertical, mais previsível e sempre legível.
- **`garantirEspaco(doc, y, alturaNecessariaMm, orientacaoPagina)`**: `desenharBarraTitulo` é desenho manual (não pagina sozinho); se vier logo antes de um `autoTable` que SIM pagina sozinho, dava pra a barra colar no rodapé da página e a tabela inteira (cabeçalho incluído) pular pra uma página nova quase em branco — bug real, já visto em produção. `garantirEspaco` checa ANTES de desenhar a barra se sobra altura suficiente pra ela + pelo menos 1 linha da tabela; se não sobrar, força a quebra de página primeiro, então barra+tabela sempre ficam juntas. Usado antes de "Lista Detalhada de Peças" e "Sobras" em `desenharPaginaDoPlano`, e antes de "Observações"/assinatura na página de orçamento.
- **Miniatura real na coluna "Imagem"** (`desenharThumbnailPeca` em `nestingPdf.js`, plugado via callback `renderizarImagem` em `desenharTabelaPecasDoPlano` — o template genérico não sabe desenhar peça, só oferece o gancho): desenha a peça "parada" (sem posição/rotação de chapa), reaproveitando `contornoDxfAbsoluto` com um placement identidade (`x=0,y=0,rotated=false`) pra reusar a mesma classificação externo/buraco sem duplicar lógica. Desenha também os FUROS por cima (reais do DXF quando existem, senão `calcularFuros` procedural) — já teve um bug onde a miniatura desenhava só o contorno e nunca os furos.
- **Tempo de corte por peça é RATEADO de verdade, não estimado**: cada linha de `desenharTabelaPecasDoPlano` mostra `tempo unitário da peça (detalhamento_pecas[id].tempo_min ÷ peca.qtd) × qtdNesting` (quantas unidades dela cabem NESSA chapa específica) — não é o tempo do orçamento inteiro. O "Tempo Corte" da tabela de especificações do plano (`desenharTabelaEspecificacoesChapa`) é a SOMA desses valores rateados de todas as peças do plano — não `item.tempo_min / chapas_necessarias` (que já foi usado por engano numa versão anterior: isso dava a média da ESPESSURA inteira, errado quando a espessura tem mais de um "Plano de Corte" com arranjos/tempos diferentes).
- **Sobras reais** (`nestingUtils.js::calcularSobrasAproveitaveis`): algoritmo de retângulos livres estilo "guillotine" — parte da área útil da chapa, vai descontando cada peça posicionada (split em até 4 tiras por peça), filtra só retângulos com os dois lados **≥100x100mm**, remove os totalmente contidos em outro, e por fim faz uma seleção gulosa por área (maior primeiro, descartando qualquer candidato que ainda se sobreponha a um já aceito) — garante que a lista final NUNCA tem sobreposição entre si, ou seja, o mesmo pedaço físico de chapa nunca é reportado duas vezes. Regra de negócio (`nestingPdf.js::desenharPaginaDoPlano`): se `item.sobra_reservada_cliente` for `false`, nem calcula — mostra "A sobra ficará na empresa." Se for `true`, lista os retângulos encontrados (Medida/Quantidade×multiplicador da chapa/Peso Unitário/Peso Total, peso pela fórmula padrão área×espessura×densidade); se não achar nada ≥100x100, cai no texto "Nenhuma sobra aproveitável neste plano."
  - **Bug crítico já corrigido, não reintroduzir**: a poda de retângulos "contidos em outro" (`podarContidos`) precisa rodar A CADA placement processado, não só uma vez no final. Sem isso, a lista de retângulos livres cresce explosivamente em chapas densas (uma peça pode dividir vários retângulos livres em até 4 cada) — confirmado por teste isolado: sem poda incremental, 600 placements numa grade densa já geravam 200 mil+ retângulos livres em poucas centenas de iterações, e o cálculo é 100% síncrono (trava a aba inteira do navegador — "Página sem resposta"). Com poda a cada passo, 6000+ placements rodam em <500ms. Existe ainda uma trava de segurança (`SOBRA_LIMITE_RETANGULOS_LIVRES = 300`): se mesmo podando a lista passar disso, desiste de listar sobra pra aquela chapa específica (degrada pro texto "nenhuma sobra aproveitável" em vez de arriscar travar de novo) — o resto do PDF continua gerando normalmente.
- Orientação de página: `retrato` por padrão (parâmetro `orientacao`, toggle na tela perto dos botões "Baixar PDF"), mas ainda existe um fallback automático pra `paisagem` quando a proporção da chapa é muito larga (`chapa_largura / chapa_comprimento > 1.4`) — independente da preferência do usuário, evita conteúdo ilegível numa chapa extrema.
- **Última página = orçamento comercial "Lypsyos"** (`desenharPaginaOrcamento` em `nestingPdf.js`, chamada uma vez ao final de `gerarPdfNesting`, sempre — não é opcional): réplica em jsPDF/autoTable do MESMO layout que já existia na versão impressa via `window.print` da tela "Resultado" (cabeçalho com wordmark "LYPSYOS" + tag pacote-completo/facção + tabela Orçamento/Máquina/Cliente/Emissão à direita, barra escura "Resumo de Custos...", grid de 4 métricas com Valor Venda Bruto destacado, tabela "Dados da Peça e Produção Detalhada" — ID/Dimensões/Furos/Qtd/Peso/Chapas/Tempo/Custo Máquina, agrupada por espessura com linha TOTAL, observações de prazo, linha de assinatura/carimbo). **Propositalmente NÃO usa `relatorioIndustrialPdf.js`** (que é o padrão "industrial" genérico das páginas de plano de corte) — tem sua própria identidade visual de marca (`desenharBarraTituloEscura`, texto direto), porque é literalmente o documento comercial que vai pro cliente, não o padrão reutilizável entre ferramentas. O PDF via `window.print` da tela "Resultado" continua existindo à parte, sem mudança — esta página final é uma segunda via, dentro do MESMO arquivo que os planos de corte, pro orçamentista poder mandar tudo num PDF só.

## Impressão do orçamento (`window.print`, tela "Resultado")

CSS em `App.css` (`@media print`). **Armadilha já resolvida uma vez, documentar pra não reabrir**: o tema escuro (`document.documentElement.classList.add('dark')`) usa CSS custom properties (`--text-heading`, `--surface-card` etc, redefinidas dentro do seletor `.dark`) que **não são removidas na impressão** — a media query `print` não tira classes do DOM. Um `body { background: white !important }` sozinho NÃO resolve, porque elementos com `background`/`color` próprios (`.surface-card`, `.surface-heading`, `.input-field`...) não herdam do `body`. A correção certa é redefinir as mesmas variáveis dentro de `@media print { .dark { ... valores claros ... } }`, garantindo que o PDF impresso sai sempre no padrão claro independente do tema ativo na tela.

## Nesting — toggle de labels na tela

`App.jsx`, estado `mostrarLabelsNesting` (botão perto dos controles de PDF na "Visualização de Nesting") — liga/desliga o texto do ID desenhado sobre cada peça no `ChapaSVG`. Complementa (não substitui) o filtro por tamanho mínimo que já existia (`p.width > chapa_largura*0.08 && p.height > chapa_comprimento*0.03`) pra texto não vazar de peças pequenas.

## Convenções de código

- Backend e frontend são monólitos de um arquivo por design até agora — só extrair módulo novo (como `nesting.py`, ou os módulos de PDF do frontend) quando a lógica for autocontida o suficiente para não precisar ficar importando meio-a-meio com o arquivo principal.
- **Padrão de "template genérico x orquestrador específico"** (estabelecido com `relatorioIndustrialPdf.js` x `nestingPdf.js`): quando um pedaço de UI/documento é pensado pra ser reaproveitado fora do GeoQuote (outras ferramentas do nicho ferro/aço), ele vira um módulo que só conhece formas/dados genéricos (sem "peça"/"orçamento"/"nesting" no vocabulário), e um segundo módulo — esse sim GeoQuote-específico — monta os dados e chama o genérico em sequência. Repetir esse padrão se surgir outro documento/tela com a mesma vocação de virar padrão da empresa.
- Nomes de variáveis/labels em português (domínio de negócio brasileiro: impostos, chapas, furos etc.), nomes de infraestrutura (funções fetch, constantes) em português também, seguindo o padrão já estabelecido no arquivo.
- CRUD upsert pela chave natural (não por id) é o padrão em todas as rotas de cadastro — mantenha essa convenção em rotas novas.
- Peça DXF-importada carrega hoje até 3 blobs auxiliares no objeto client-side (`dxfPreviewSvg`, `contornoDxf`, e os campos escalares de área/perímetro) — nenhum deles vai pro backend em `/calcular-orcamento` (Pydantic ignora campos extras por padrão, então mandar não quebraria nada, mas também não teria efeito), e continuam não fazendo parte do que é persistido em `orcamentos.payload` (ver "Contas, Assinaturas e Times") — só sobrevivem enquanto a aba do navegador está aberta. O orçamento em si (peças com preço/tempo já resolvidos + resultado) já é salvo; especificamente esses 3 blobs de preview/contorno DXF, não.
